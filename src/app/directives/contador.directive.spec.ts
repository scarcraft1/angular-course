import { ContadorDirective } from './contador.directive';

describe('ContadorDirective', () => {
  it('should create an instance', () => {
    const directive = new ContadorDirective();
    expect(directive).toBeTruthy();
  });
});
